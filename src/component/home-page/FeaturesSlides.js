import React from "react";

import classes from "./FeaturesSlides.module.css";

const FeaturesSlides = () => {
  return (
    <div className={`${classes["FeaturesSlides"]}`}>
      
    </div>
  );
};

export default FeaturesSlides;